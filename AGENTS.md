没有我的额外要求，你必须执行以下要求：

1. 不要修改readme文件，不要在项目里新建文档。
2. 不要在根目录新建文件（你自己的临时文件事后可以删除的我不管）
3. 案例里的文件结构不要破坏，不要新增文件，保持plot,run和modflow_model三者，run作为唯一入口，plot直接绘图。
4. 不要再plot里新增注释和标题。
5. 不要给各案例增加在examples里的共用脚本。